@model Debcore.Model.Party
 
 @{
     ViewBag.Title = "title";
     Layout = "_Layout";
 }
 
 <h2>@Model.Name</h2>
 
 @foreach (var debsDetail in @Model.GetDebsDetails())
 {
     <div>@debsDetail.ToString()</div>
 }

<Party name="test"></Party>
